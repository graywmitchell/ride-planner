describe('calcAdjustment', () => {
    it('should increase adjustedDuration when fitness level gets worse', () => {
        const userWeightBefore = 150;
        const userWeightAfter = 250;
        const duration = ;
        const feetVal = ;
        const inchesVal = ;
        const userAge = ;
        const userFitness = ;
        expect(calcAdjustment(duration, feetVal, inchesVal, userWeightBefore, userAge, userFitness)).toBe(false);
    });